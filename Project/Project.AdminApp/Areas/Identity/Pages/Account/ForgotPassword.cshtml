@page   "/quen-mat-khau/"
@model ForgotPasswordModel
@{
    ViewData["Title"] = "Quên mật khẩu?";
}


<div class="row">
    <div class="container-fluid">
        <h1>@ViewData["Title"]</h1>
        <h4>Nhập email của tài khoản.</h4>
        <hr />
        <div class="col-md-6">
            <form method="post">
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Input.Email"></label>
                    <input asp-for="Input.Email" class="form-control" id="fisrtEmail" />
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-primary">Gửi</button>
            </form>
            @{ string error = ViewData["notFoundEmail"] as String;
                if (error != null)
                {
    <form method="get" action="./Register">
        <hr />
        <h4>Bạn có muốn tạo tài khoản với email này không ?</h4>
        <hr />
        <input asp-for="Input.Email" name="preEmail" style="display:none" />
        <button type="submit" class="btn btn-primary">Đăng kí</button>
    </form> } }

        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    
}